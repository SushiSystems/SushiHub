[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "sushicore"
version = "0.1.0"
description = "Shared core for the Sushi* developer CLIs (hub / sr / se / sa / sb / sd): workspace resolution, layered config, presentation, and the cmake/ctest driver."
authors = [{ name = "Mustafa Garip", email = "mustafagarip@sushisystems.io" }]
requires-python = ">=3.10"
dependencies = [
    "rich>=13.0",
    "tomli>=2.0; python_version < '3.11'",
]

[project.optional-dependencies]
test = ["pytest>=7.0"]

[tool.setuptools.packages.find]
where = ["."]
include = ["sushicore*"]

[tool.setuptools.package-data]
sushicore = ["py.typed"]
