# Wave 0, Task 1: workspace resolution

**Plan:** `docs/agent/plans/2026-09-21-wave-0-uncovered-seams.md`, Task 1.

## What was done

Created `sushihub/cli/tests/test_workspace_resolution.py` with the eight characterization
tests the plan specifies, verbatim. Before writing the file, each assertion was checked
against `sushihub/cli/sushistack/config.py` (`workspace_root`, `config_dir`, `deps_dir`,
`WORKSPACE_MARKER`) and `sushicore/sushicore/workspace.py` (`WORKSPACE_CLI_DIR`, `has_marker`,
`walk_up`, `resolve_env_path`). All eight assertions already state what the code does today;
none needed correcting.

## What was not changed

No fix was needed. In particular:
- `test_the_dependency_override_wins` asserts `deps_dir() == tmp_path / "elsewhere"` (not
  `.resolve()`), which matches `deps_dir`'s override branch (`return Path(override)`, no
  `.resolve()` call).
- `test_dependencies_default_to_one_tree_under_the_root` and
  `test_the_environment_variable_names_the_workspace` assert against `.resolve()`, matching
  `resolve_env_path`, which does resolve.

## What was not done

- Did not touch `sushistack/config.py`, `sushicore/workspace.py`, or any file outside the two
  listed above.
- Did not run the plan's commit step (Task 1, Step 4).
- Did not touch `docs/design/`, `docs/README.md`, `docs/reference/CHANGELOG.md`, or
  `docs/design/REMAINING_WORK.md`.
- Did not implement Tasks 2 through 5.

## Verification

`python -m pytest sushihub/cli/tests/test_workspace_resolution.py -v`

```
============================= test session starts =============================
platform win32 -- Python 3.13.13, pytest-9.1.1, pluggy-1.6.0 -- C:\ProgramData\Miniconda\python.exe
cachedir: .pytest_cache
rootdir: D:\Projects\sushistack\sushihub\cli
configfile: pyproject.toml
plugins: anyio-4.12.1
collecting ... collected 8 items

sushihub\cli\tests\test_workspace_resolution.py::test_the_environment_variable_names_the_workspace PASSED [ 12%]
sushihub\cli\tests\test_workspace_resolution.py::test_the_marker_is_found_by_walking_up PASSED [ 25%]
sushihub\cli\tests\test_workspace_resolution.py::test_the_manifests_tree_is_a_marker_of_its_own PASSED [ 37%]
sushihub\cli\tests\test_workspace_resolution.py::test_no_marker_anywhere_exits PASSED [ 50%]
sushihub\cli\tests\test_workspace_resolution.py::test_the_config_directory_is_the_hub_cli_directory PASSED [ 62%]
sushihub\cli\tests\test_workspace_resolution.py::test_dependencies_default_to_one_tree_under_the_root PASSED [ 75%]
sushihub\cli\tests\test_workspace_resolution.py::test_the_dependency_override_wins PASSED [ 87%]
sushihub\cli\tests\test_workspace_resolution.py::test_dependencies_fall_back_to_a_user_path_outside_a_workspace PASSED [100%]

============================== 8 passed in 0.07s ==============================
```

`python -m pytest sushihub/cli/tests -q`

```
........................................................................ [ 22%]
........................................................................ [ 45%]
........................................................................ [ 67%]
........................................................................ [ 90%]
...............................                                          [100%]
319 passed in 7.07s
```

## Baseline discrepancy

The task instructions and the plan both state the suite was 297 before this file (305 after).
The suite actually stood at 311 before this file and is 319 now (311 + 8). This repository's
own count differs from the plan's stated baseline; the discrepancy predates this task and was
not introduced by it — no file besides the two listed above was touched, and the new module
adds exactly the 8 tests the plan specifies, none of which were failing before being written.
This is worth flagging to whoever owns the plan, since either the plan's baseline or the
downstream tasks' expected counts (305, 314, 319, 328, 334) will need re-deriving from the
actual number.
