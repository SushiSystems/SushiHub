# Wave 1, Task 1: Make sushicore publishable, in place

**Status:** Steps 1 to 4 done. Steps 5 (backlog edit) and 6 (commit) left to the orchestrator, as instructed.

## Files changed

- Created `sushicore/README.md`
- Modified `sushicore/pyproject.toml`

No other file was touched. `sushicore/.gitignore` was not changed; it already excludes `dist/`
and `*.egg-info/`, and both were confirmed absent from `git status` after the build.

## Step 1: The front-door README

Written from the plan's draft, checked against `sushicore/docs/README.md` and the real package
tree. Two points needed verification rather than a copy:

- **Module table.** Every module the plan lists (`workspace`, `config_base`, `console`,
  `renderer`, `events`, `theme`, `icons`, `cmake_driver`, `cmake_cache`, `proc`,
  `toolchain_args`) exists under `sushicore/sushicore/`. The table is accurate as drafted; no
  correction needed.
- **Console script.** `sushicore/pyproject.toml` carries no `[project.scripts]` table, so "it
  installs no console script" is correct as drafted.

One thing the plan's draft asserts that I could not verify from this repository alone: that all
seven CLIs (`hub`, `sr`, `se`, `sa`, `sb`, `sd`, `st`) import `sushicore`. `sushicore`'s own
`__init__.py` docstring names only six (`sr`, `se`, `hub`, `sa`, `sb`, `sd`), omitting `st`
(sushitrack). The wave 1 plan's own Global Constraints section states sushitrack uses
`sushicore` today, so I kept the draft's seven-CLI claim rather than the package docstring's
six-CLI one; that docstring is a separate piece of prose this task's file list does not cover
and I left it unchanged. Worth the orchestrator's eye if the two are meant to agree.

Given the humanizer pass: the draft was already terse and factual (specific module names, no
evaluative adjectives, no closing summary), so it is used verbatim.

## Step 2: Packaging metadata

Added `readme`, `license`, `classifiers` and `[project.urls]` to `sushicore/pyproject.toml`
exactly as the plan specifies, directly after the `description` line.

## Step 3: Build and twine check

```
$ python -m build sushicore
...
adding 'sushicore-0.1.0.dist-info/licenses/LICENSE'
adding 'sushicore-0.1.0.dist-info/METADATA'
adding 'sushicore-0.1.0.dist-info/WHEEL'
adding 'sushicore-0.1.0.dist-info/top_level.txt'
adding 'sushicore-0.1.0.dist-info/RECORD'
removing build\bdist.win-amd64\wheel
WARNING `project.license` as a TOML table is deprecated
[... setuptools deprecation notice about project.license, SPDX string form, deadline 2027-02-18 ...]
Successfully built sushicore-0.1.0.tar.gz and sushicore-0.1.0-py3-none-any.whl
```

```
$ python -m twine check sushicore/dist/*
Checking sushicore/dist/sushicore-0.1.0-py3-none-any.whl: PASSED
Checking sushicore/dist/sushicore-0.1.0.tar.gz: PASSED
```

Both artifacts pass. The `license = { file = "LICENSE" }` table form the plan specifies triggers
a setuptools deprecation warning (table form is deprecated in favour of an SPDX string, removed
2027-02-18); it does not fail the build or `twine check`, so step 3's acceptance is met as
written. Flagging it here rather than silently changing the plan's specified form.

## Step 4: Tests and clean tree

```
$ python -m pytest sushicore/tests -q
........................................................................ [ 76%]
......................                                                   [100%]
94 passed in 0.31s
```

```
$ git status --porcelain sushicore/
 M sushicore/pyproject.toml
?? sushicore/README.md
```

Only the two files this task was scoped to touch appear. `sushicore/dist/` and any
`*.egg-info` directory do not appear; `sushicore/.gitignore` already excludes them, so no
`.gitignore` edit was needed.

## Departures from the plan's draft

None in substance. The README text and the pyproject additions were used as drafted; the only
open question is the six-vs-seven-CLI count noted under Step 1, which is a pre-existing
inconsistency between the plan's draft and `sushicore/__init__.py`'s docstring, not something
this task's file list authorizes fixing.

## Not done

- Step 5 (deleting the README-placement item from `docs/design/REMAINING_WORK.md`) — left to the
  orchestrator per this dispatch's instructions.
- Step 6 (the commit) — left to the orchestrator per this dispatch's instructions.
